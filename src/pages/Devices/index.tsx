import { ReactElement } from 'react'
import './index.css'

const Devices = (): ReactElement => {
  return (
    <div className="devices">
      <h2>Devices</h2>
      {/* 디바이스 관련 내용을 여기에 추가 */}
    </div>
  )
}

export default Devices
